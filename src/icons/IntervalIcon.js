import React from 'react';
import Icon from 'react-native-vector-icons/Entypo';

const IntervalIcon = ({size, color}) => {
  return <Icon name="stopwatch" size={size} color={color} />;
};

export default IntervalIcon;
