import React from 'react';
import Icon from 'react-native-vector-icons/AntDesign';

const RightArrow = ({size, color}) => {
  return <Icon name="right" size={size} color={color} />;
};

export default RightArrow;
