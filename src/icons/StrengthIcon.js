import React from 'react';
import Icon from 'react-native-vector-icons/MaterialCommunityIcons';

const StrengthIcon = ({color, size}) => {
  return <Icon name="weight-lifter" color={color} size={size} />;
};

export default StrengthIcon;
